Branch: `record_status` is `estimate_sent`, `appointment_booked`, or `approved`. The customer already has an owner-approved price on this thread, and the intake step has already sent the owner the durable `customer-replied` alert for this message; do not send another. Do not reopen the specification gate, do not price again, and do not request price approval again. Exactly one of these outcomes finishes the claim: an acknowledged continuation, a rendering sent in the thread, an appointment approval filed for the owner, or manual review.

A. Classify the newest customer message against the immutable approved design. Identify every explicit intent in it so a combined rendering-and-appointment request is not mistaken for something else. Write `work_paths.thread_review` as JSON with exactly: `thread_id` (the owned Gmail thread ID); `source_message_id` (`<CLAIMED_GMAIL_ID>`); `message_ids` (every Gmail message ID in the fetched thread in chronological order, including shop messages); `missing_required_fields` (an empty array); and `post_estimate_artifact`, an object with exactly `design_change_assessment`, `intents`, and `changed_fields`. Omit `specification`; the helper preserves the approved specification from the authoritative record. `design_change_assessment` is one of `unchanged`, `changed`, or `uncertain`; use `uncertain` whenever the newest wording might alter the approved design but cannot be mapped confidently to a specification field, including a request for a second or different piece. `intents` are unique values chosen only from `estimate_acceptance`, `rendering_request`, and `appointment_request`; include every explicit intent, and never downgrade clear rendering or appointment wording to `uncertain` merely because both appear together. `changed_fields` holds lowercase field keys only when the assessment is `changed`; otherwise it is empty. Exact shapes: rendering only `{"design_change_assessment":"unchanged","intents":["rendering_request"],"changed_fields":[]}`; appointment only `{"design_change_assessment":"unchanged","intents":["appointment_request"],"changed_fields":[]}`; both `{"design_change_assessment":"unchanged","intents":["rendering_request","appointment_request"],"changed_fields":[]}`; plain acceptance `{"design_change_assessment":"unchanged","intents":["estimate_acceptance"],"changed_fields":[]}`. Price pushback, a discount request, anger, or any escalation is manual review (Finishing), not a classification.

Run exactly `python3 <BASE_DIR>/scripts/estimate_record.py record-thread-review --estimate-id '<ESTIMATE_ID>' --snapshot '<work_paths.thread_review>' --shop-profile <WORKSPACE>/estimate-desk/shop-profile.json --record-root <WORKSPACE>/estimate-desk/records --output '<work_paths.current_record>'`, then exactly `python3 <BASE_DIR>/scripts/workflow_safe.py finalize-post-estimate --monitor-root <WORKSPACE>/estimate-desk/inbox-monitor --claim-root <WORKSPACE>/estimate-desk/inbox-claims --record-root <WORKSPACE>/estimate-desk/records --message-id '<CLAIMED_GMAIL_ID>' --estimate-id '<ESTIMATE_ID>' --record-output '<work_paths.current_record>'`. It mirrors the review, finalizes acknowledgements and acceptances, terminalizes `changed`, `uncertain`, or malformed classifications to manual review itself, and returns the only allowed `next_action`: `finalize` or `manual_review` mean the claim is already finished, reply with exactly `NO_REPLY`; `request_appointment_approval` means B; `send_rendering` means C; `request_appointment_approval_then_send_rendering` means B with `--defer-finalize-for-rendering` and then C. Follow it exactly. Never retry a changed, uncertain, or malformed classification with rewritten JSON.

B. Appointment intent at Stage 1 or 2 (from `next_action`). Create a durable owner approval instead of booking or promising anything. Write exactly `{"requested_times":["customer-provided timing, if any"],"calendar_availability":[{"start":"ISO timestamp","end":"ISO timestamp","label":"owner-timezone label"}]}` to `work_paths.appointment_intent`; either array may be empty, and never invent requested times or availability. Add live, validated slots to `calendar_availability` only when both `scheduling.calendar` and at least one `scheduling.windows` entry are configured: run exactly `python3 <BASE_DIR>/scripts/calendar_query.py --time-min '<ISO timestamp with timezone>' --time-max '<later ISO timestamp with timezone>' --timezone '<profile scheduling.timezone>' --calendar-id '<profile scheduling.calendar>' --output '<work_paths.calendar_receipt>'` (it has no `--window-days`), write candidate slots derived only from the declared windows inside those bounds to `work_paths.calendar_candidate_slots`, run exactly `python3 <BASE_DIR>/scripts/appointment_options.py '<work_paths.calendar_receipt>' '<work_paths.calendar_candidate_slots>' '<work_paths.calendar_options>' --timezone '<profile scheduling.timezone>' --window-days '<profile scheduling.meeting_offer_window_days>'`, and copy slots only from the validated `work_paths.calendar_options`. Never infer that a meeting exists from email wording; the calendar is the only source of truth. Then run exactly `python3 <BASE_DIR>/scripts/workflow_safe.py request-appointment-approval --monitor-root <WORKSPACE>/estimate-desk/inbox-monitor --claim-root <WORKSPACE>/estimate-desk/inbox-claims --record-root <WORKSPACE>/estimate-desk/records --message-id '<CLAIMED_GMAIL_ID>' --estimate-id '<ESTIMATE_ID>' --appointment-intent '<work_paths.appointment_intent>' --appointment-approval '<work_paths.appointment_approval>' --record-output '<work_paths.current_record>'`. For an appointment-only message it finalizes the claim; reply with exactly `NO_REPLY`. For a message that also requests a rendering, add `--defer-finalize-for-rendering` and continue with C, whose `send-rendering` finalizes the claim. Never say the appointment is booked until its approval is granted and the live calendar write succeeds; the main session handles that after approval. Do not call `notify-owner-claimed` for the appointment.

C. Rendering intent (from `next_action`). A rendering after a sent estimate is an in-scope continuation and needs no new approval. Read `<BASE_DIR>/templates/customer-emails.md` for the rendering note wording. Generate exactly two complementary-view PNG illustrations of the same approved design in parallel with `image_generate`, never alternate design proposals, never CAD or manufacturing files. `image_generate` completes asynchronously: until both completion events arrive or the bounded budget is exhausted, run exactly `python3 <BASE_DIR>/scripts/rendering_wait.py wait --monitor-root <WORKSPACE>/estimate-desk/inbox-monitor --claim-root <WORKSPACE>/estimate-desk/inbox-claims --message-id '<CLAIMED_GMAIL_ID>'`; after each wait returns, collect completion events already delivered to this session, otherwise repeat the exact command. The helper permits at most eight fixed 30-second waits. A pending rendering is never a valid final response: never promise to send it later, return success, or leave the claim processing. Compare each completed candidate with the immutable approved specification and discard any that visibly changes the piece type, metal color, silhouette, rail or shank layout, setting topology, stone location or coverage, center-stone type or shape, stone count, requested view, or a `DO NOT CHANGE` constraint. When both events arrive, continue with one conforming candidate if the other is wrong, or both when both conform; if the eighth wait returns `exhausted:true`, continue with at least one completed, conforming candidate. If no candidate completed, run the manual-review command with reason `rendering_generation_timeout`; if candidates completed but none conforms, with reason `rendering_validation_failed`; then reply with exactly `NO_REPLY` and send nothing. For each conforming image run exactly `python3 <BASE_DIR>/scripts/rendering_materialize.py --monitor-root <WORKSPACE>/estimate-desk/inbox-monitor --claim-root <WORKSPACE>/estimate-desk/inbox-claims --message-id '<CLAIMED_GMAIL_ID>' --source '<image_generate-managed-media-path>' --slot 1` (`--slot 2` for the second); never use `cp`, `mv`, `curl`, or generic binary writes. Write the price-free rendering note to `work_paths.customer_reply` using only the customer-safe approved specification: it must say the written specification controls the final piece and must not mention costs, assumptions, CAD, or manufacturing design files. Then run exactly `python3 <BASE_DIR>/scripts/workflow_safe.py send-rendering --monitor-root <WORKSPACE>/estimate-desk/inbox-monitor --claim-root <WORKSPACE>/estimate-desk/inbox-claims --record-root <WORKSPACE>/estimate-desk/records --message-id '<CLAIMED_GMAIL_ID>' --estimate-id '<ESTIMATE_ID>' --body '<work_paths.customer_reply>' --image '<work_paths.rendering_image_1>' [--image '<work_paths.rendering_image_2>'] --gmail-payload '<work_paths.gmail_payload>' --provider-response '<work_paths.gmail_provider_response>' --record-output '<work_paths.current_record>'`. It delivers the images once in the owned Gmail thread (the claimed Gmail message ID is the iteration and idempotency key), persists and mirrors the record, and finalizes the claim; reply with exactly `NO_REPLY`.

