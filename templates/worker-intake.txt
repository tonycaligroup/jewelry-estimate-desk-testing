Branch: `record_status` is `awaiting_specs`. No estimate has been sent on this thread. You make at most two judgments, the review and, when pricing, a few quantities; every other step is one command. Exactly one of these outcomes finishes the claim: not an inquiry, a specification follow-up, a question to the owner for a missing rate, a price approval request, or manual review.

A. Is it a quote request? Read `thread.messages` from the `worker-start` result. If the thread is not a request for a custom piece, replica, redesign, remount, or repair (for example a supplier, marketing, a personal note, a job application, or an unrelated question), run exactly `python3 <BASE_DIR>/scripts/workflow_safe.py not-an-inquiry --monitor-root <WORKSPACE>/estimate-desk/inbox-monitor --claim-root <WORKSPACE>/estimate-desk/inbox-claims --record-root <WORKSPACE>/estimate-desk/records --message-id '<CLAIMED_GMAIL_ID>' --estimate-id '<ESTIMATE_ID>' --reason <not_a_quote_request|vendor_or_marketing|personal_or_internal|unrelated> --record-output '<work_paths.current_record>'`; it retires the record, mirrors it, and finalizes the claim with no customer reply; reply with exactly `NO_REPLY`. Escalations are never `not-an-inquiry`; they are manual review (Finishing). A repair, resize, or restring continues as intake. An appraisal, insurance valuation, existing-inventory price, or job-status question is manual review with reason `not_an_estimate_request`.

B. Review. Merge every customer-supplied fact from every customer message in the thread into one normalized, customer-safe `specification` object: `piece_type`, `quantity`, `metal`, `metal_karat`, `metal_color`, `stone_type`, `stone_origin` (`natural` or `lab-grown`), `stone_shape`, `stone_carat`, `stone_color`, `stone_clarity`, `stone_cut`, `stone_count`, `finger_size` or `dimensions`, `setting_style`, `finish`, `engraving`, `event_date`, `budget`, `customer_supplied_materials`, `certificate`, `reference_images`, `scheduling_intent`; include only keys the customer actually answered. A photo can inform a question but never satisfies a required field. Map descriptive design wording such as classic band, solitaire, channel-set, bezel-set, or a delegated style choice into `setting_style`; never write `not specified`, `unspecified`, `unknown`, `tbd`, or `n/a` as a value, and if no real description or explicit delegation exists leave `setting_style` out. Color, clarity, cut, finish, and similar quality choices are complete when the customer explicitly delegates them to the jeweler (record the delegation, for example `"stone_color": "jeweler's choice"`); never ask for a choice they delegated. Shop sourcing is the default unless the customer says they are supplying the stone or metal. Budget and event date are useful, never required. Then list `missing_required_fields`: the required keys still absent after the merge, or an empty array. Required keys: `stone_type`, `stone_origin`, `stone_carat`, `stone_color`, `stone_clarity`, `stone_cut` when the piece has a stone; `metal`, `metal_karat`, `metal_color`; `finger_size` for a ring or `dimensions` for a chain, bracelet, or pendant; `piece_type`, `quantity`, `setting_style`. The helper may add `stone_origin` when the shop profile says to ask always; treat what it returns as authoritative and never re-review the same message to change it.

Write `{"specification": {...}, "missing_required_fields": [...]}` to `<WORK_DIR>/review.json`, then run exactly `python3 <BASE_DIR>/scripts/workflow_safe.py review-thread --monitor-root <WORKSPACE>/estimate-desk/inbox-monitor --claim-root <WORKSPACE>/estimate-desk/inbox-claims --record-root <WORKSPACE>/estimate-desk/records --shop-profile <WORKSPACE>/estimate-desk/shop-profile.json --message-id '<CLAIMED_GMAIL_ID>' --estimate-id '<ESTIMATE_ID>' --review '<WORK_DIR>/review.json'`. It adds the thread ids itself, records the review, and then runs everything that needs no judgment: it fetches the spot metal price when enabled, builds the cost skeleton, and, when a rate is missing from the card, asks the owner and parks the claim. Read its `next` and do exactly that:

- `next` is `done`: the command already finished the claim (it asked the owner for a rate, or filed a manual review). Reply with exactly `NO_REPLY`.
- `next` is `send_spec_followup`: go to C with the returned `missing_required_fields`, `initiating`, `customer_reply`, and `route`.
- `next` is `price`: go to D with the returned `fill`, `fee_catalog`, `stone_catalog`, and `typical_finished_weights`.

If `worker-start` returned a `resume` object instead, skip A and B and go straight to C with `resume.missing_required_fields` and `resume.initiating`, using `work_paths.customer_reply` and `work_paths.route`.

C. Specification follow-up. Read `<BASE_DIR>/templates/spec-gate-email.md` and write one friendly, price-free, batched request for exactly the missing fields to the `customer_reply` path; do not re-ask anything the customer already said, and do not mention costs, rates, or assumptions. Offer appointment slots only when both `scheduling.calendar` and at least one `scheduling.windows` entry are configured in the shop profile; otherwise omit slots and do not call `calendar_query.py`. When both are configured, run exactly `python3 <BASE_DIR>/scripts/calendar_query.py --time-min '<ISO timestamp with timezone>' --time-max '<later ISO timestamp with timezone>' --timezone '<profile scheduling.timezone>' --calendar-id '<profile scheduling.calendar>' --output '<work_paths.calendar_receipt>'` (it has no `--window-days`), write candidate slots derived only from the declared windows inside those bounds to `work_paths.calendar_candidate_slots`, run exactly `python3 <BASE_DIR>/scripts/appointment_options.py '<work_paths.calendar_receipt>' '<work_paths.calendar_candidate_slots>' '<work_paths.calendar_options>' --timezone '<profile scheduling.timezone>' --window-days '<profile scheduling.meeting_offer_window_days>'`, and offer only slots from the validated `work_paths.calendar_options`, with timezone. Then run exactly `python3 <BASE_DIR>/scripts/workflow_safe.py send-spec-followup --monitor-root <WORKSPACE>/estimate-desk/inbox-monitor --claim-root <WORKSPACE>/estimate-desk/inbox-claims --record-root <WORKSPACE>/estimate-desk/records --message-id '<CLAIMED_GMAIL_ID>' --estimate-id '<ESTIMATE_ID>' --route '<route path>' --body '<customer_reply path>' --gmail-payload '<work_paths.gmail_payload>' --provider-response '<work_paths.gmail_provider_response>' --record-output '<work_paths.current_record>'`, adding `--initiating` only when `initiating` is true. It sends in the original Gmail thread, persists and mirrors the record, and finalizes the claim; reply with exactly `NO_REPLY`. Do not stop after identifying the missing fields and do not ask the owner whether to send. A thread that still cannot complete after two rounds of questions is manual review with reason `specification_incomplete_after_followup`.

D. Price. The skeleton is already built. Decide only the quantities, estimated deliberately high: finished grams of metal (use the matching `typical_finished_weights` entry when there is one and the design is ordinary), bench hours, the center-stone carat only if `fill` lists it as missing, which entries of `fee_catalog` apply (for example casting and setting for a new piece), and any accent stones as `stone_catalog` key plus total carats. Then run exactly `python3 <BASE_DIR>/scripts/workflow_safe.py price --monitor-root <WORKSPACE>/estimate-desk/inbox-monitor --claim-root <WORKSPACE>/estimate-desk/inbox-claims --record-root <WORKSPACE>/estimate-desk/records --shop-profile <WORKSPACE>/estimate-desk/shop-profile.json --message-id '<CLAIMED_GMAIL_ID>' --estimate-id '<ESTIMATE_ID>' --finished-grams <grams> --bench-hours <hours> [--center-carat <carats>] [--fee <fee_catalog key>]... [--accent <stone_catalog key>:<total carats>]...`. It fills the skeleton, derives every rate and the price from the card and the pricing model, creates the binding and the owner's approval brief, persists `pending_approval`, mirrors the record, and finalizes the claim. If it refuses, correct only the quantity or catalog key it names and run it again; never edit the skeleton, the rates, or the price by hand, and never send anything to the customer: the estimate goes out only after the owner approves. Reply with exactly `NO_REPLY`.
