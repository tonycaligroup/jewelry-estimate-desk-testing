Branch: `record_status` is `awaiting_specs`. No estimate has been sent on this thread. Exactly one of these four outcomes finishes the claim: not an inquiry, a specification follow-up, a question to the owner for a missing rate, or a price approval request.

A. Is it a quote request? If the thread is not a request for a custom piece, replica, redesign, remount, or repair (for example a supplier, marketing, a personal note, a job application, or an unrelated question), run exactly `python3 <BASE_DIR>/scripts/workflow_safe.py not-an-inquiry --monitor-root <WORKSPACE>/estimate-desk/inbox-monitor --claim-root <WORKSPACE>/estimate-desk/inbox-claims --record-root <WORKSPACE>/estimate-desk/records --message-id '<CLAIMED_GMAIL_ID>' --estimate-id '<ESTIMATE_ID>' --reason <not_a_quote_request|vendor_or_marketing|personal_or_internal|unrelated> --record-output '<work_paths.current_record>'`. It retires the record, mirrors it, and finalizes the claim with no customer reply and no owner alert; then reply with exactly `NO_REPLY`. Escalations are never `not-an-inquiry`; they are manual review (Finishing). A repair, resize, or restring continues as intake with no rendering. An appraisal, insurance valuation, existing-inventory price, or job-status question is manual review with reason `not_an_estimate_request`.

B. Review the whole thread. Merge every customer-supplied fact from the initiating inquiry and all later customer replies into one normalized, customer-safe specification: piece type and quantity; metal, karat, and color; stone type, natural or lab-grown, shape, carat, color, clarity, cut, and count; finger size for rings or length/dimensions for chains, bracelets, and pendants; setting and style; finish; engraving; event date and budget (useful, never required); customer-supplied materials; certificate; reference images; and scheduling intent. A photo can inform a question but never satisfies a required field. Map descriptive design wording such as classic band, solitaire, channel-set, bezel-set, or a delegated style choice into `setting_style`; never record `not specified`, `unspecified`, `unknown`, `tbd`, or `n/a` as a completed setting/style, and if no real description or explicit delegation exists keep `setting_style` missing. Color, clarity, cut, finish, and similar quality choices are complete when the customer explicitly delegates them to the jeweler; do not ask the customer to specify a choice they delegated. Shop sourcing is the default unless the customer says they are supplying the stone or metal. The profile-backed helper keeps `stone_origin` missing when `defaults.stone_origin` is `ask_always` unless the customer explicitly chose natural or lab-grown; price sensitivity and `delegated_to_jeweler` never satisfy it. For a piece without stones, stone fields are not applicable. Required fields are: stone type, origin, carat, color, clarity, and cut/shape (when the piece has a stone); metal, karat, and color; finger size or dimensions; piece type, quantity, and setting/style.

Write `work_paths.thread_review` as JSON with exactly: `thread_id` (the owned Gmail thread ID); `source_message_id` (`<CLAIMED_GMAIL_ID>`); `message_ids` (every Gmail message ID in the fetched thread in chronological order, including shop messages); `specification` (the merged specification); and `missing_required_fields` (only the applicable required field keys still absent after the merge, or an empty array). Then run exactly `python3 <BASE_DIR>/scripts/estimate_record.py record-thread-review --estimate-id '<ESTIMATE_ID>' --snapshot '<work_paths.thread_review>' --shop-profile <WORKSPACE>/estimate-desk/shop-profile.json --record-root <WORKSPACE>/estimate-desk/records --output '<work_paths.current_record>'`. Treat the returned `missing_required_fields` as authoritative: never rewrite or retry the same source-message thread review to clear, replace, or invent a returned missing field. Never re-ask a fact present anywhere in the thread.

C. Specification follow-up (returned `missing_required_fields` is not empty). Read `<BASE_DIR>/templates/spec-gate-email.md` and write one friendly, price-free, batched request for exactly the missing fields to `work_paths.customer_reply`; do not re-ask known facts and do not mention costs, rates, or assumptions. Offer appointment slots only when both `scheduling.calendar` and at least one `scheduling.windows` entry are configured in the shop profile; otherwise omit slots, do not call `calendar_query.py`, and do not treat their absence as an error. When both are configured, run exactly `python3 <BASE_DIR>/scripts/calendar_query.py --time-min '<ISO timestamp with timezone>' --time-max '<later ISO timestamp with timezone>' --timezone '<profile scheduling.timezone>' --calendar-id '<profile scheduling.calendar>' --output '<work_paths.calendar_receipt>'` (it has no `--window-days`), write candidate slots derived only from the declared windows inside those bounds to `work_paths.calendar_candidate_slots`, run exactly `python3 <BASE_DIR>/scripts/appointment_options.py '<work_paths.calendar_receipt>' '<work_paths.calendar_candidate_slots>' '<work_paths.calendar_options>' --timezone '<profile scheduling.timezone>' --window-days '<profile scheduling.meeting_offer_window_days>'`, and offer only slots from the validated `work_paths.calendar_options`, with timezone. Then immediately run exactly `python3 <BASE_DIR>/scripts/workflow_safe.py send-spec-followup --monitor-root <WORKSPACE>/estimate-desk/inbox-monitor --claim-root <WORKSPACE>/estimate-desk/inbox-claims --record-root <WORKSPACE>/estimate-desk/records --message-id '<CLAIMED_GMAIL_ID>' --estimate-id '<ESTIMATE_ID>' --route '<work_paths.route>' --body '<work_paths.customer_reply>' --gmail-payload '<work_paths.gmail_payload>' --provider-response '<work_paths.gmail_provider_response>' --record-output '<work_paths.current_record>'`, adding `--initiating` only when the intake result's `decision` is `new_inquiry` (the claimed message created the estimate) or `resume.initiating` is true. It sends in the original Gmail thread, persists and mirrors the record, and finalizes the claim; reply with exactly `NO_REPLY`. Do not stop after identifying the missing fields, and do not ask the owner whether to draft, send, or continue. After one partial reply, ask once more only for load-bearing gaps; a thread that still cannot complete after that is manual review with reason `specification_incomplete_after_followup`.

D. Price (returned `missing_required_fields` is empty). The only authorized steps are pricing through the helper and the owner-approval request; do not query the calendar, generate renderings, or ask the owner what to build. When `pricing.spot_metal.enabled` is true in the shop profile, first run exactly `python3 <BASE_DIR>/scripts/spot_price.py` with the cache, provider, frequency, metal, currency, and unit from the shop profile and `--output '<WORK_DIR>/spot-evidence.json'`; if it fails, stop pricing (manual review, reason `spot_price_unavailable`) rather than substitute a remembered or stale price. Then run exactly `python3 <BASE_DIR>/scripts/cost_components.py prepare --record-root <WORKSPACE>/estimate-desk/records --estimate-id '<ESTIMATE_ID>' --shop-profile <WORKSPACE>/estimate-desk/shop-profile.json --output '<WORK_DIR>/cost-skeleton.json'`, adding `--spot-evidence '<WORK_DIR>/spot-evidence.json'` when spot pricing is enabled. Read the skeleton.

If `unresolved` is not empty, the shop has no rate for that line. That is a question for the owner, not a review and not a guess: run exactly `python3 <BASE_DIR>/scripts/workflow_safe.py ask-missing-rate --monitor-root <WORKSPACE>/estimate-desk/inbox-monitor --claim-root <WORKSPACE>/estimate-desk/inbox-claims --record-root <WORKSPACE>/estimate-desk/records --shop-profile <WORKSPACE>/estimate-desk/shop-profile.json --message-id '<CLAIMED_GMAIL_ID>' --estimate-id '<ESTIMATE_ID>'`. It asks the owner in plain words, parks and finalizes this claim; reply with exactly `NO_REPLY`. Never ask the owner yourself, never substitute, estimate, or infer a rate, and never file a manual review for a missing rate.

Otherwise fill only the fields the skeleton lists under `fill` (finished grams, bench hours, a missing carat weight), estimated deliberately high; add fee lines only by copying entries from `fee_catalog`, and accent-stone lines only by copying `stone_catalog` entries with a quantity. Never edit a `rate_key`, `unit_cost`, `spot_price_per_gram`, `purity`, `rate`, or `proposed_price`; the helper owns them. Write the filled skeleton back to the same path, then run exactly `python3 <BASE_DIR>/scripts/cost_components.py finalize --input '<WORK_DIR>/cost-skeleton.json' --shop-profile <WORKSPACE>/estimate-desk/shop-profile.json --output '<work_paths.current_state>'`. It re-derives every rate from the card, computes the proposed price with the configured pricing model, and writes the exact current state that `request-approval` accepts. If it refuses, correct only the quantity or line it names and run it again; never rewrite rates or the price and never edit the current state by hand. A line the helper still refuses is manual review with reason `invalid_cost_components`.

Then run exactly `python3 <BASE_DIR>/scripts/workflow_safe.py request-approval --monitor-root <WORKSPACE>/estimate-desk/inbox-monitor --claim-root <WORKSPACE>/estimate-desk/inbox-claims --record-root <WORKSPACE>/estimate-desk/records --message-id '<CLAIMED_GMAIL_ID>' --estimate-id '<ESTIMATE_ID>' --current-state '<work_paths.current_state>' --approval-request '<work_paths.approval_request>' --shop-profile <WORKSPACE>/estimate-desk/shop-profile.json --record-output '<work_paths.current_record>'`. It creates the binding with the exact proposed price and owner-only cost sheet, files the owner's approval brief, persists `pending_approval`, mirrors the record, and finalizes the claim; if a retry finds an existing approval request it reuses that exact artifact, so never delete or rewrite it. Do not send anything to the customer: the estimate goes out only after the owner approves, through the main session. Do not add a separate `notify-owner` call for the approval. Reply with exactly `NO_REPLY`.
