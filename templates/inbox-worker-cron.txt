Process exactly one claimed Jewelry Estimate Desk message, `<CLAIMED_GMAIL_ID>` for estimate `<ESTIMATE_ID>`, exactly as follows. The watcher job has already discovered, claimed, fetched, classified, routed, and recorded it; you do only the judgment work for this one claim.

Never call create_goal or update_goal. Goals do not apply in isolated cron sessions.

This is a fail-closed procedure, not an open-ended task. Use only the available `exec`, `read`, `write`, and `image_generate` tools. Use `image_generate` only for an explicit post-estimate customer request for a visual rendering. With `exec`, run only the exact bundled-script commands required by the installed SKILL.md, a reference it explicitly directs you to read, or this worker checklist. Never run `python3 -c`, `gws`, `curl`, a shell-sourced credential file, an inline script, a direct JSON-edit command, or any command you invent. Never read the bundled scripts' source code: no `grep`, `sed`, `cat`, `head`, `tail`, or `read` of anything under `<BASE_DIR>/scripts`; a helper's `--help` output and its error message are the only interface, and an error you cannot satisfy is a manual-review outcome, not a research task. All Gmail reads must go through `gmail_fetch.py`; all Gmail sends must go through `workflow_safe.py`. Do not create or choose paths. If an exact required command cannot be run, stop with a real error and no further tools or side effects. Never substitute a plausible command.

The allowed tool set is complete for this workflow. `exec` runs the bundled scripts that provide Gmail access, same-thread customer replies, Kolo record persistence, owner notifications, approvals, and calendar operations. `read` reads the installed instructions, fetched Gmail, and generated JSON artifacts, and `write` creates only the model-authored JSON and reply-body artifacts at the exact `work_paths` returned by `worker-start`. Do not expect or request separate Gmail, messaging, database, persistence, or thread-review tools. Their absence is not an error while the required bundled command and its required input artifacts are available. Before step 1, read the installed jewelry-estimate-desk-testing SKILL.md completely and follow it as the authoritative workflow, including every reference it explicitly requires for the active branch. Use this cron message as the execution checklist and exact-command supplement. Never stop by listing available tools or asking the owner to provide commands already defined by the installed skill or this worker checklist.

1. Run exactly `python3 <BASE_DIR>/scripts/inbox_monitor.py status`. If activation state is not `active`, reply with exactly `NO_REPLY` and do nothing else. Then run exactly `python3 <BASE_DIR>/scripts/workflow_safe.py worker-start --monitor-root <WORKSPACE>/estimate-desk/inbox-monitor --claim-root <WORKSPACE>/estimate-desk/inbox-claims --message-id '<CLAIMED_GMAIL_ID>'`. It proves this claim is still leased to you and prints the intake result: `estimate_id`, `record_status`, `thread_message_count`, and the only supported `work_paths` (the fetched message and thread are already at `work_paths.gmail_message` and `work_paths.gmail_thread`). If it exits nonzero, reply with exactly `NO_REPLY`; the watcher recovers the claim. Never run `claim-next`, `discover`, `fetch-claimed`, `intake`, `notification-reconcile-stale`, or `reconcile-stale-claims`; never process any other message.

2. `record_status` tells you which review shape to write: `awaiting_specs` means no estimate has been sent yet; `estimate_sent`, `appointment_booked`, or `approved` means this is a reply after an estimate and you write the post-estimate continuation. Use the returned `work_paths` exactly. Never copy or choose a claim token, create `.jed-work`, `/tmp/jed-work`, or another ad-hoc work directory; never inspect or edit `claim.json`, `state.json`, queue files, or record JSON directly; and never use generic show/display/read-back calls for generated artifacts. Pass generated artifact paths directly between bundled scripts. Record the required claim phases described in this runbook. Ownership candidates, the ownership decision with the exact Gmail thread message count, and the retry-stable record for a `new_inquiry` (persisted locally and mirrored to Kolo before any customer reply or terminal claim) are all produced by `workflow_safe.py intake`; never rebuild them by hand. Before any external action, identify every intent in the newly claimed message so a combined rendering-and-appointment reply is not mistaken for a new estimate. Read the fetched full Gmail thread, read every message in chronological order, and first decide whether this is a quote request at all. When `record_status` is `awaiting_specs` and the thread is not a request for a custom piece, replica, redesign, remount, or repair (for example a supplier, marketing, a personal note, a job application, or an unrelated question), run exactly `python3 <BASE_DIR>/scripts/workflow_safe.py not-an-inquiry --monitor-root <WORKSPACE>/estimate-desk/inbox-monitor --claim-root <WORKSPACE>/estimate-desk/inbox-claims --record-root <WORKSPACE>/estimate-desk/records --message-id '<claimed-gmail-id>' --estimate-id '<jed-id>' --reason <not_a_quote_request|vendor_or_marketing|personal_or_internal|unrelated> --record-output '<work_paths.current_record>'`, which retires the record, mirrors it, and finalizes the claim with no customer reply and no owner alert; then reply with exactly `NO_REPLY`. Anger, legal, payment, appraisal, or other escalations are not `not-an-inquiry`: they remain manual review. Otherwise merge all customer-supplied facts and write the model-authored `work_paths.thread_review` JSON before running `estimate_record.py record-thread-review` with every thread message ID plus the currently claimed source ID and `--shop-profile <WORKSPACE>/estimate-desk/shop-profile.json`. Treat the returned `missing_required_fields` as authoritative: never rewrite or retry the same source-message thread review to clear, replace, or invent a returned missing field. The profile-backed helper must keep `stone_origin` missing when `defaults.stone_origin` is `ask_always` unless the customer explicitly selected natural or lab-grown; price sensitivity and `delegated_to_jeweler` never satisfy that policy. For a sent estimate, record a post-estimate continuation: omit `specification`, keep `missing_required_fields` empty, and include `post_estimate_artifact` with exactly `design_change_assessment`, `intents`, and `changed_fields` as defined in step 9; use `uncertain` if the new message might alter the approved design. The helper preserves the approved specification and derives its hash from the authoritative record. After the required `customer-replied` owner alert has been sent, run exactly `python3 <BASE_DIR>/scripts/workflow_safe.py finalize-post-estimate --monitor-root <WORKSPACE>/estimate-desk/inbox-monitor --claim-root <WORKSPACE>/estimate-desk/inbox-claims --record-root <WORKSPACE>/estimate-desk/records --message-id '<claimed-gmail-id>' --estimate-id '<jed-id>' --record-output '<work_paths.current_record>'`. Follow its `next_action` exactly. Do not retry a changed, uncertain, or malformed classification with rewritten JSON. The appointment and rendering commands revalidate the persisted source-message, thread, approved-specification hash, estimate, and authorized intent before acting. Do not reopen the specification gate or request price approval again unless a later manual workflow explicitly handles a design change. For every real customer reply, send the durable `customer-replied` owner alert before invoking a high-level workflow command that finalizes the claim. Never evaluate only the newest email or re-ask a fact present anywhere in the thread. When required specifications remain missing at trust Stage 2 or 3, the supported branch is: write `work_paths.thread_review`, persist it with `estimate_record.py record-thread-review`, write the price-free body to `work_paths.customer_reply`, then immediately run `workflow_safe.py send-spec-followup` so it sends in the original Gmail thread, persists and mirrors the record, and finalizes the claim. Do not stop after identifying the missing fields, and do not ask whether to draft, send, or continue routing. When `record-thread-review` returns `specs_complete` with empty `missing_required_fields` and no estimate has been sent, the only authorized next steps are to price through `cost_components.py prepare`, fill its quantities, run `cost_components.py finalize`, and call `workflow_safe.py request-approval`; do not query the calendar, generate renderings, or ask the owner what to build. When `pricing.spot_metal` is enabled, first run exactly `python3 <BASE_DIR>/scripts/spot_price.py` with the cache, provider, frequency, metal, currency, and unit from the shop profile and `--output '<work_paths dir>/spot-evidence.json'`. Then run exactly `python3 <BASE_DIR>/scripts/cost_components.py prepare --record-root <WORKSPACE>/estimate-desk/records --estimate-id '<jed-id>' --shop-profile <WORKSPACE>/estimate-desk/shop-profile.json --output '<work_paths dir>/cost-skeleton.json'`, adding `--spot-evidence '<work_paths dir>/spot-evidence.json'` when spot pricing is enabled. Read the skeleton. Fill only the fields it lists under `fill` (finished grams, bench hours, a missing carat weight), estimated high. Add fee lines only by copying entries from `fee_catalog`, and accent-stone lines only by copying `stone_catalog` entries with a quantity. Never edit a `rate_key`, `unit_cost`, `spot_price_per_gram`, `purity`, `rate`, or `proposed_price`; the helper owns them. If `unresolved` is not empty, the shop has no single configured rate for that line: never substitute, estimate, or infer one; fail closed to manual review with reason `invalid_cost_components`. Write the filled skeleton back to the same path, then run exactly `python3 <BASE_DIR>/scripts/cost_components.py finalize --input '<work_paths dir>/cost-skeleton.json' --shop-profile <WORKSPACE>/estimate-desk/shop-profile.json --output '<work_paths.current_state>'`. It re-derives every rate from the card, computes the proposed price with the configured pricing model, and writes the exact current state that `request-approval` accepts. If it refuses, correct only the quantity or line it names and run it again; never rewrite rates or the price, never edit the current state by hand, and never read the scripts' source to work out a format. Then call `workflow_safe.py request-approval`. These high-level commands resolve the authoritative claim token, journal the external action, persist and mirror the record, advance phases, and finalize. Do not substitute `gmail_safe.py`, `kolo_safe.py`, direct Gmail/Maton calls, or direct JSON edits. Never tell the owner to calculate or send manually. Never overwrite initial immutable evidence. Notification or `awaiting_specs` alone is not completion.
Customer-delegated quality choices are complete, not missing; use the shop's default owner-only pricing assumptions. Budget is optional, and shop sourcing is the default unless the customer says otherwise. Do not ask the customer to specify a choice they delegated to the jeweler. For a specification follow-up, omit appointment slots and do not call `calendar_query.py` unless both `scheduling.calendar` and at least one `scheduling.windows` entry are configured; when configured, use only the exact calendar and appointment-options commands in the installed SKILL.md and the calendar paths returned by `worker-start`. Never invent `--window-days` for `calendar_query.py`.
After an estimate has been sent, an explicit customer request for a visual rendering is an in-scope continuation and needs no new estimate approval. Generate exactly two complementary-view PNG illustrations of the same approved design in parallel, never alternate design proposals. `image_generate` completes asynchronously: until both completion events arrive or the bounded budget is exhausted, run exactly `python3 <BASE_DIR>/scripts/rendering_wait.py wait --monitor-root <WORKSPACE>/estimate-desk/inbox-monitor --claim-root <WORKSPACE>/estimate-desk/inbox-claims --message-id '<claimed-gmail-id>'`. After each wait returns, collect completion events already delivered to this same session; otherwise repeat the exact command. The helper permits at most eight fixed 30-second waits. A pending rendering is not a valid final response: never promise to send it later, return success, or leave the claim processing. Compare each completed candidate with the immutable approved specification and discard any candidate that visibly changes the piece type, metal color, silhouette, rail or shank layout, setting topology, stone location or coverage, center-stone type or shape, stone count, requested view, or a `DO NOT CHANGE` constraint. When both events arrive, continue with one conforming candidate if the other is wrong, or both when both conform. If the eighth wait returns `exhausted:true`, continue with at least one completed, conforming candidate rather than blocking on the other. If no candidate completed, run exactly `python3 <BASE_DIR>/scripts/kolo_safe.py manual-review-claimed --monitor-root <WORKSPACE>/estimate-desk/inbox-monitor --claim-root <WORKSPACE>/estimate-desk/inbox-claims --message-id '<claimed-gmail-id>' --reason-code rendering_generation_timeout`; if candidates completed but none conforms, run that exact command with reason `rendering_validation_failed`. Then reply with exactly `NO_REPLY`; do not send a customer email or appointment-success message. For each conforming candidate, run the exact `rendering_materialize.py` command reproduced in step 9 to copy it into slot 1 or 2; never use `cp`, `mv`, `curl`, or generic binary writes. If the same message also requests an appointment at Stage 1 or 2, create its durable appointment approval with step 8 and `--defer-finalize-for-rendering` before calling `workflow_safe.py send-rendering`. Then deliver the selected image or images once in the owned Gmail thread with `workflow_safe.py send-rendering`; the claimed Gmail message ID is the rendering iteration and idempotency key. The rendering remains approval-free; the separate calendar action waits for owner approval. Only Stage 3 may confirm a live-calendar write inside declared windows. Do not create a new estimate, perform another specification gate, mention CAD, or mention manufacturing design files.

5. The owner alert in step 4 must use only `python3 <BASE_DIR>/scripts/kolo_safe.py notify-owner-claimed`, which atomically performs notification write-ahead and records `sent` or `uncertain`. Never call `kolo notify-owner` directly for that event, never send it after the claim has been finalized, and never independently retry `pending` or `uncertain` notifications.
6. Never call `inbox_claim.py complete`, `inbox_claim.py fail`, `reconcile-terminal`, or `inbox_monitor.py finalize` separately after either successful high-level workflow command; it already finalizes. A send/build/persistence failure must become manual review, never processed. For every manual-review decision use the `kolo_safe.py manual-review-claimed` command reproduced in step 9. Conflicting terminal outcomes remain errors.
7. The claim is finished only when a high-level `workflow_safe.py` command or `manual-review-claimed` has finalized it. Then reply with exactly `NO_REPLY` and nothing else; owner-facing results reach the owner through approvals, alerts, and the watcher's report, never through your reply. Never run `assert-settled` or `run-report`. A nonzero result from a required command that you cannot satisfy is manual review with the fixed reason it names, never a retry with rewritten input.

8. For every Stage 1/2 appointment intent on an estimate that has already been sent, create a durable Kolo approval instead of relying on the cron's final chat delivery. Do not call `notify-owner-claimed` for that appointment action. Write exactly `{\"requested_times\":[\"customer-provided timing, if any\"],\"calendar_availability\":[{\"start\":\"ISO timestamp\",\"end\":\"ISO timestamp\",\"label\":\"owner-timezone label\"}]}` to `work_paths.appointment_intent`; either array may be empty, but never invent requested times or availability. Run exactly `python3 <BASE_DIR>/scripts/workflow_safe.py request-appointment-approval --monitor-root <WORKSPACE>/estimate-desk/inbox-monitor --claim-root <WORKSPACE>/estimate-desk/inbox-claims --record-root <WORKSPACE>/estimate-desk/records --message-id '<claimed-gmail-id>' --estimate-id '<jed-id>' --appointment-intent '<work_paths.appointment_intent>' --appointment-approval '<work_paths.appointment_approval>' --record-output '<work_paths.current_record>'`. For a combined rendering-and-appointment message, add `--defer-finalize-for-rendering`, then complete `send-rendering`, which finalizes the claim. For an appointment-only message, the appointment command finalizes the claim itself. Then reply with exactly `NO_REPLY`; the approval is durable and actionable even if cron announce delivery fails. Never claim the appointment is booked until its approval is granted and the live calendar write succeeds. Stage 3 remains autonomous inside declared windows and does not create this approval.
9. These exact operational definitions supplement the installed skill. The post-estimate artifact must contain exactly `design_change_assessment`, `intents`, and `changed_fields`. Assessment is one of `unchanged`, `changed`, or `uncertain`. Intents are unique values chosen only from `estimate_acceptance`, `rendering_request`, and `appointment_request`. `changed_fields` contains lowercase field keys only when assessment is `changed`; otherwise it is empty. For rendering only write `{"design_change_assessment":"unchanged","intents":["rendering_request"],"changed_fields":[]}`. For appointment only write `{"design_change_assessment":"unchanged","intents":["appointment_request"],"changed_fields":[]}`. For a combined request write exactly `{"design_change_assessment":"unchanged","intents":["rendering_request","appointment_request"],"changed_fields":[]}`. Include every explicit intent in the newly claimed message. Clear rendering or appointment wording must not be downgraded to `uncertain` merely because both intents appear together.

When `cost_components.py prepare` reports a non-empty `unresolved`, the shop has no rate for that line: run exactly `python3 <BASE_DIR>/scripts/workflow_safe.py ask-missing-rate --monitor-root <WORKSPACE>/estimate-desk/inbox-monitor --claim-root <WORKSPACE>/estimate-desk/inbox-claims --record-root <WORKSPACE>/estimate-desk/records --shop-profile <WORKSPACE>/estimate-desk/shop-profile.json --message-id '<CLAIMED_GMAIL_ID>' --estimate-id '<ESTIMATE_ID>'`. It asks the owner in plain words, parks and finalizes this claim; then reply with exactly `NO_REPLY`. Never guess a rate, never ask the owner yourself, and never file a manual review for a missing rate.
For every manual-review decision run exactly `python3 <BASE_DIR>/scripts/kolo_safe.py manual-review-claimed --monitor-root <WORKSPACE>/estimate-desk/inbox-monitor --claim-root <WORKSPACE>/estimate-desk/inbox-claims --message-id '<claimed-gmail-id>' --reason-code '<fixed_reason>'`. For each conforming generated image run exactly `python3 <BASE_DIR>/scripts/rendering_materialize.py --monitor-root <WORKSPACE>/estimate-desk/inbox-monitor --claim-root <WORKSPACE>/estimate-desk/inbox-claims --message-id '<claimed-gmail-id>' --source '<image_generate-managed-media-path>' --slot 1`; use `--slot 2` for the second conforming image. Use these reproduced commands exactly; do not search for or invent an alternate command.
For pre-estimate specification intake, map the customer's descriptive design wording—such as classic band, solitaire, channel-set, bezel-set, or a delegated style choice—into `setting_style`, `setting`, `style`, or `design_style`. Never record `not specified`, `unspecified`, `unknown`, `tbd`, or `n/a` as a completed setting/style. If no real description or explicit delegation exists, keep `setting_style` in `missing_required_fields` and use the normal specification follow-up branch.
When authoring a customer body, after reading the installed skill and any reference it explicitly requires for the active branch, read only `templates/spec-gate-email.md` for a specification request or `templates/customer-emails.md` for a rendering note. Do not browse unrelated reference documents.
Deterministic classifier exits (`auto_reply`, `dsn_candidate`, unsupported) are handled inside `workflow_safe.py intake`; do not run separate classifier, completion, or manual-review commands for them.
