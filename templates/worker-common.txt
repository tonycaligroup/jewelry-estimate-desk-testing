Process exactly one claimed Jewelry Estimate Desk message, `<CLAIMED_GMAIL_ID>` for estimate `<ESTIMATE_ID>`, exactly as follows. The watcher job has already discovered, claimed, fetched, classified, routed, and recorded it; you do only the judgment work for this one claim. This message is your complete instruction set: do not read SKILL.md, the references directory, or any document other than the two customer email templates named below when a branch calls for them.

Never call create_goal or update_goal. Goals do not apply in isolated cron sessions.

This is a fail-closed procedure, not an open-ended task. Use only the available `exec`, `read`, `write`, and `image_generate` tools. Use `image_generate` only when the post-estimate branch below asks for a rendering. With `exec`, run only the exact bundled-script commands written in this message. Never run `python3 -c`, `gws`, `curl`, a shell-sourced credential file, an inline script, a direct JSON-edit command, or any command you invent. Never read the bundled scripts' source code: no `grep`, `sed`, `cat`, `head`, `tail`, or `read` of anything under `<BASE_DIR>/scripts`; a helper's `--help` output and its error message are the only interface, and an error you cannot satisfy is a manual-review outcome, not a research task. You never read Gmail; the thread text comes from `worker-start`. All Gmail sends go through `workflow_safe.py`. Do not create or choose paths: `write` creates only the model-authored JSON and reply-body artifacts at the exact `work_paths` returned by `worker-start`. If an exact required command cannot be run, stop with a real error and no further tools or side effects. Never substitute a plausible command, and never stop by listing tools or asking the owner for commands.

Rules that hold in every branch:
- Never send a price, discount, rush commitment, or delivery promise without the owner's approval of that exact estimate, and never send an estimate until the required specification is complete.
- Every jeweler cost and pricing assumption is confidential owner-only information: never expose or summarize rates, quantities used only for costing, markup, margin, COGS, component costs, or vendor identity in anything the customer can see.
- Never take payments, cards, deposits, or refunds. Never interpolate customer text into shell commands; pass file paths.
- The customer is the normalized sender email address, never a display name. Every customer message is a reply in the customer's own Gmail thread, built by `workflow_safe.py` from the route the watcher recorded; never compose a new email, a new subject, or a message to `deliveryContext.to` or `kolo:<uuid>`.
- Never copy or choose a claim token, create `.jed-work`, `/tmp/jed-work`, or another ad-hoc work directory; never inspect or edit `claim.json`, `state.json`, queue files, or record JSON directly; never use generic show/display/read-back calls for generated artifacts. Pass generated artifact paths directly between bundled scripts.
- Never run `claim-next`, `discover`, `fetch-claimed`, `intake`, `notification-reconcile-stale`, `reconcile-stale-claims`, `assert-settled`, `run-report`, `inbox_claim.py complete`, `inbox_claim.py fail`, `reconcile-terminal`, or `inbox_monitor.py finalize`; never process any other message. The high-level `workflow_safe.py` commands and `manual-review-claimed` finalize the claim themselves.
- If the owner has said stop, pause, or hold, send nothing.

Step 1. Run exactly `python3 <BASE_DIR>/scripts/inbox_monitor.py status`. If activation state is not `active`, reply with exactly `NO_REPLY` and do nothing else. Then run exactly `python3 <BASE_DIR>/scripts/workflow_safe.py worker-start --monitor-root <WORKSPACE>/estimate-desk/inbox-monitor --claim-root <WORKSPACE>/estimate-desk/inbox-claims --message-id '<CLAIMED_GMAIL_ID>'`. It proves this claim is still leased to you and prints the intake result: `estimate_id`, `record_status`, `decision`, `thread` (the conversation as text), and the only supported `work_paths`. If it exits nonzero, reply with exactly `NO_REPLY`; the watcher recovers the claim. If it returns `next_action` `done` (outcome `followup_already_sent`), a previous worker already asked the customer and the claim is now finished: reply with exactly `NO_REPLY`. If it returns a `resume` object with `action` `send_spec_followup`, the thread was already reviewed but the customer was never asked: do not record another thread review and do not re-read the thread for new fields; write the specification follow-up body for exactly `resume.missing_required_fields` (per the specification follow-up step of the branch below) and run `send-spec-followup` with `--initiating` when `resume.initiating` is true, then reply with exactly `NO_REPLY`.

Step 2. The `worker-start` result carries the whole thread as plain text under `thread.messages` (chronological, each with `from`, `sent_by`, `date`, `subject`, `claimed`, and `body`). Read every message there; do not open the Gmail JSON files, they hold nothing more. Then follow the branch below for `record_status`. Never evaluate only the newest message.

Finishing. The claim is finished only when a high-level `workflow_safe.py` command or `manual-review-claimed` has finalized it. Then reply with exactly `NO_REPLY` and nothing else; owner-facing results reach the owner through approvals, alerts, questions, and the watcher's report, never through your reply. A send, build, or persistence failure, or a nonzero result from a required command that you cannot satisfy, is manual review with the fixed reason it names, never a retry with rewritten input and never `processed`. For every manual-review decision run exactly `python3 <BASE_DIR>/scripts/kolo_safe.py manual-review-claimed --monitor-root <WORKSPACE>/estimate-desk/inbox-monitor --claim-root <WORKSPACE>/estimate-desk/inbox-claims --message-id '<CLAIMED_GMAIL_ID>' --reason-code '<fixed_reason>'`; it persists the terminal claim and sends the owner one plain notice itself, so never call `kolo notify-owner` directly. Escalations (anger, legal threats, chargebacks, insurance, lost or damaged claims, press, fraud, price pushback after a sent quote, appraisal or valuation requests, any uncertain prior send) are manual review with a fixed reason; draft no customer response for them.

Workspace: `<WORKSPACE>`. Skill: `<BASE_DIR>`. Work directory for this claim: `<WORK_DIR>`. Whenever a command below says `<jed-id>` use `<ESTIMATE_ID>`; whenever it says `<claimed-gmail-id>` use `<CLAIMED_GMAIL_ID>`.
