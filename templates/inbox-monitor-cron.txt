Run the Jewelry Estimate Desk inbox monitor exactly as follows.

Never call create_goal or update_goal. Goals do not apply in isolated cron sessions.

This is a fail-closed procedure, not an open-ended task. Use only the available `exec`, `read`, `write`, and `image_generate` tools. Use `image_generate` only for an explicit post-estimate customer request for a visual rendering. With `exec`, run only the exact bundled-script commands required below. Never run `python3 -c`, `gws`, `curl`, a shell-sourced credential file, an inline script, a direct JSON-edit command, or any command you invent. All Gmail reads must go through `gmail_fetch.py`; all Gmail sends must go through `workflow_safe.py`. Do not create or choose paths. If an exact required command cannot be run, stop with a real error and no further tools or side effects. Never substitute a plausible command.

The allowed tool set is complete for this workflow. `exec` runs the bundled scripts that provide Gmail access, same-thread customer replies, Kolo record persistence, owner notifications, approvals, and calendar operations. `read` reads the fetched Gmail and generated JSON artifacts, and `write` creates only the model-authored JSON and reply-body artifacts at the exact `work_paths` returned by `claim-next`. Do not expect or request separate Gmail, messaging, database, persistence, or thread-review tools. Their absence is not an error while the required bundled command and its required input artifacts are available. Never stop by listing the available tools or asking the owner to provide commands that are already defined here or in the installed SKILL.md.

1. Run exactly `python3 <BASE_DIR>/scripts/validate_profile.py <WORKSPACE>/estimate-desk/shop-profile.json`, then exactly `python3 <BASE_DIR>/scripts/inbox_monitor.py status`. If profile validation is not ready, or activation state is `prepared` or `reconfiguring`, exit successfully with `NO_REPLY` and do nothing else. Missing, corrupt, unsupported, or active-state-invalid data is a real error: fail closed without Gmail or customer side effects. Never call `prepare-run`; the deterministic Gmail discovery helper owns its private run directory and batch.
2. Before Gmail discovery, run `python3 <BASE_DIR>/scripts/inbox_claim.py notification-reconcile-stale --minimum-age-seconds 600`, then `python3 <BASE_DIR>/scripts/kolo_safe.py reconcile-stale-claims --monitor-root <WORKSPACE>/estimate-desk/inbox-monitor --claim-root <WORKSPACE>/estimate-desk/inbox-claims --minimum-age-seconds 600`. The first command never delivers or retries a notification. The second terminalizes delivery-ambiguous, legacy, or same-phase retry-exhausted stale claims as manual review before one generic alert; a safely journaled claim receives at most one automatic retry at each phase.
3. Run exactly `python3 <BASE_DIR>/scripts/gmail_fetch.py discover --monitor-root <WORKSPACE>/estimate-desk/inbox-monitor`. This helper alone captures the window end, applies the one-second watermark overlap, queries the Maton-routed Gmail API, paginates, validates provider metadata, writes the private discovery batch, commits it, and advances the watermark only after a complete successful enumeration. Never perform Gmail discovery yourself and never call `discover-complete` directly. A nonzero result is a real run failure; stop without processing.
4. Repeatedly call `python3 <BASE_DIR>/scripts/inbox_monitor.py claim-next --claim-root <WORKSPACE>/estimate-desk/inbox-claims --stale-after-seconds 600`. This one command selects the oldest eligible item, claims or safely resumes it, synchronizes the queue, and returns the only supported persistent `work_paths`; never split those operations. Stop the loop when it returns `null`. Process only when `claim.acquired` is `true`; `claim.resumed:true` replays from durable state. Immediately run exactly `python3 <BASE_DIR>/scripts/gmail_fetch.py fetch-claimed --monitor-root <WORKSPACE>/estimate-desk/inbox-monitor --claim-root <WORKSPACE>/estimate-desk/inbox-claims --message-id '<gmail-id returned by claim-next>'`. This helper is the only permitted Gmail read for a claimed item and writes the exact full message and full thread to the returned `work_paths.gmail_message` and `work_paths.gmail_thread`. Use the returned `work_paths` exactly. Never copy or choose a claim token, create `.jed-work`, `/tmp/jed-work`, or another ad-hoc work directory; never inspect or edit `claim.json`, `state.json`, queue files, or record JSON directly; and never use generic show/display/read-back calls for generated artifacts. Pass generated artifact paths directly between bundled scripts. Follow the installed SKILL.md rules exactly and record its required claim phases. Build ownership candidates only with `estimate_record.py lookup-thread`, and use the exact Gmail thread message count from `work_paths.gmail_thread` with `route_ownership.py`. A `new_inquiry` must have its retry-stable record persisted locally and mirrored to Kolo before any customer reply or terminal claim. Before any external action, identify every intent in the newly claimed message so a combined rendering-and-appointment reply is not mistaken for a new estimate. Read the fetched full Gmail thread, read every message in chronological order, merge all customer-supplied facts, and write the model-authored `work_paths.thread_review` JSON before running `estimate_record.py record-thread-review` with every thread message ID plus the currently claimed source ID. For a sent estimate, record a post-estimate continuation: omit `specification`, keep `missing_required_fields` empty, and include `post_estimate_artifact` with exactly `design_change_assessment`, `intents`, and `changed_fields` as defined in SKILL.md; use `uncertain` if the new message might alter the approved design. The helper preserves the approved specification and derives its hash from the authoritative record. After the required `customer-replied` owner alert has been sent, run exactly `python3 <BASE_DIR>/scripts/workflow_safe.py finalize-post-estimate --monitor-root <WORKSPACE>/estimate-desk/inbox-monitor --claim-root <WORKSPACE>/estimate-desk/inbox-claims --record-root <WORKSPACE>/estimate-desk/records --message-id '<claimed-gmail-id>' --estimate-id '<jed-id>' --record-output '<work_paths.current_record>'`. Follow its `next_action` exactly. Do not retry a changed, uncertain, or malformed classification with rewritten JSON. The appointment and rendering commands revalidate the persisted source-message, thread, approved-specification hash, estimate, and authorized intent before acting. Do not reopen the specification gate or request price approval again unless a later manual workflow explicitly handles a design change. For every real customer reply, send the durable `customer-replied` owner alert before invoking a high-level workflow command that finalizes the claim. Never evaluate only the newest email or re-ask a fact present anywhere in the thread. When required specifications remain missing at trust Stage 2 or 3, the supported branch is: write `work_paths.thread_review`, persist it with `estimate_record.py record-thread-review`, write the price-free body to `work_paths.customer_reply`, then immediately run `workflow_safe.py send-spec-followup` so it sends in the original Gmail thread, persists and mirrors the record, and finalizes the claim. Do not stop after identifying the missing fields, and do not ask whether to draft, send, or continue routing. When the full-thread specification is complete and no estimate has been sent, calculate with the configured pricing model, create the complete owner-only cost sheet, and call `workflow_safe.py request-approval`. These high-level commands resolve the authoritative claim token, journal the external action, persist and mirror the record, advance phases, and finalize. Do not substitute `gmail_safe.py`, `kolo_safe.py`, direct Gmail/Maton calls, or direct JSON edits. Never tell the owner to calculate or send manually. Never overwrite initial immutable evidence. Notification or `awaiting_specs` alone is not completion.
Customer-delegated quality choices are complete, not missing; use the shop's default owner-only pricing assumptions. Budget is optional, and shop sourcing is the default unless the customer says otherwise. Do not ask the customer to specify a choice they delegated to the jeweler.
After an estimate has been sent, an explicit customer request for a visual rendering is an in-scope continuation and needs no new estimate approval. Generate exactly two complementary-view PNG illustrations of the same approved design in parallel, never alternate design proposals. `image_generate` completes asynchronously: until both completion events arrive or the bounded budget is exhausted, run exactly `python3 <BASE_DIR>/scripts/rendering_wait.py wait --monitor-root <WORKSPACE>/estimate-desk/inbox-monitor --claim-root <WORKSPACE>/estimate-desk/inbox-claims --message-id '<claimed-gmail-id>'`. After each wait returns, collect completion events already delivered to this same session; otherwise repeat the exact command. The helper permits at most eight fixed 30-second waits. A pending rendering is not a valid final response: never promise to send it later, return success, or leave the claim processing. Compare each completed candidate with the immutable approved specification and discard any candidate that visibly changes the piece type, metal color, silhouette, rail or shank layout, setting topology, stone location or coverage, center-stone type or shape, stone count, requested view, or a `DO NOT CHANGE` constraint. When both events arrive, continue with one conforming candidate if the other is wrong, or both when both conform. If the eighth wait returns `exhausted:true`, continue with at least one completed, conforming candidate rather than blocking on the other. If no candidate completed, run exactly `python3 <BASE_DIR>/scripts/kolo_safe.py manual-review-claimed --monitor-root <WORKSPACE>/estimate-desk/inbox-monitor --claim-root <WORKSPACE>/estimate-desk/inbox-claims --message-id '<claimed-gmail-id>' --reason-code rendering_generation_timeout`; if candidates completed but none conforms, run that exact command with reason `rendering_validation_failed`. Then run `assert-settled` and return a concise real failure; do not send a customer email or appointment-success message. For each conforming candidate, run the exact bundled `rendering_materialize.py` command in SKILL.md to copy it into slot 1 or 2; never use `cp`, `mv`, `curl`, or generic binary writes. If the same message also requests an appointment at Stage 1 or 2, create its durable appointment approval with step 8 and `--defer-finalize-for-rendering` before calling `workflow_safe.py send-rendering`. Then deliver the selected image or images once in the owned Gmail thread with `workflow_safe.py send-rendering`; the claimed Gmail message ID is the rendering iteration and idempotency key. The rendering remains approval-free; the separate calendar action waits for owner approval. Only Stage 3 may confirm a live-calendar write inside declared windows. Do not create a new estimate, perform another specification gate, mention CAD, or mention manufacturing design files.

5. The owner alert in step 4 must use only `python3 <BASE_DIR>/scripts/kolo_safe.py notify-owner-claimed`, which atomically performs notification write-ahead and records `sent` or `uncertain`. Never call `kolo notify-owner` directly for that event, never send it after the claim has been finalized, and never independently retry `pending` or `uncertain` notifications.
6. Never call `inbox_claim.py complete`, `inbox_claim.py fail`, `reconcile-terminal`, or `inbox_monitor.py finalize` separately after either successful high-level workflow command; it already finalizes. A send/build/persistence failure must become manual review, never processed. For every manual-review decision use the installed SKILL.md `kolo_safe.py manual-review-claimed` command. Conflicting terminal outcomes remain errors.
7. After `claim-next` returns null, run `python3 <BASE_DIR>/scripts/inbox_monitor.py assert-settled`. A nonzero result is a real run failure and must never be reported as `ok` or `NO_REPLY`. Stay silent with `NO_REPLY` only when this assertion passes and there is no error, manual-review item, or newly reconciled uncertain notification. Report only real errors, unresolved manual-review counts, or newly uncertain notification counts; never include customer data. For manual review, say exactly where the owner can act: `Ask Kolo to show unresolved Jewelry Estimate Desk reviews.` Never merely say `Open Kolo to review`. The final response must be either `NO_REPLY` or a concise owner-facing result; never expose internal reasoning, plans, or loop-control text.
8. For every Stage 1/2 appointment intent on an estimate that has already been sent, create a durable Kolo approval instead of relying on the cron's final chat delivery. Do not call `notify-owner-claimed` for that appointment action. Write exactly `{\"requested_times\":[\"customer-provided timing, if any\"],\"calendar_availability\":[{\"start\":\"ISO timestamp\",\"end\":\"ISO timestamp\",\"label\":\"owner-timezone label\"}]}` to `work_paths.appointment_intent`; either array may be empty, but never invent requested times or availability. Run exactly `python3 <BASE_DIR>/scripts/workflow_safe.py request-appointment-approval --monitor-root <WORKSPACE>/estimate-desk/inbox-monitor --claim-root <WORKSPACE>/estimate-desk/inbox-claims --record-root <WORKSPACE>/estimate-desk/records --message-id '<claimed-gmail-id>' --estimate-id '<jed-id>' --appointment-intent '<work_paths.appointment_intent>' --appointment-approval '<work_paths.appointment_approval>' --record-output '<work_paths.current_record>'`. For a combined rendering-and-appointment message, add `--defer-finalize-for-rendering`, then complete `send-rendering`, which finalizes the claim. For an appointment-only message, the appointment command finalizes the claim itself. Run `assert-settled` and return `NO_REPLY`; the approval is durable and actionable even if cron announce delivery fails. Never claim the appointment is booked until its approval is granted and the live calendar write succeeds. Stage 3 remains autonomous inside declared windows and does not create this approval.
