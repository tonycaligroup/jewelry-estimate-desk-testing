Run the Jewelry Estimate Desk inbox monitor exactly as follows.

Never call create_goal or update_goal. Goals do not apply in isolated cron sessions.

1. Validate <WORKSPACE>/estimate-desk/shop-profile.json. Run `python3 <BASE_DIR>/scripts/inbox_monitor.py status`. If activation_state is `prepared` or `reconfiguring`, exit successfully with `NO_REPLY` and do nothing else. Missing, corrupt, or unsupported state is a real error: fail closed without Gmail or customer side effects. Then run `python3 <BASE_DIR>/scripts/inbox_monitor.py prepare-run` and use its returned `discovery_batch` path exactly. Never create a run directory yourself.
2. Before Gmail discovery, run `python3 <BASE_DIR>/scripts/inbox_claim.py notification-reconcile-stale --minimum-age-seconds 600`, then `python3 <BASE_DIR>/scripts/kolo_safe.py reconcile-stale-claims --monitor-root <WORKSPACE>/estimate-desk/inbox-monitor --claim-root <WORKSPACE>/estimate-desk/inbox-claims --minimum-age-seconds 600`. The first command never delivers or retries a notification. The second terminalizes delivery-ambiguous, legacy, or same-phase retry-exhausted stale claims as manual review before one generic alert; a safely journaled claim receives at most one automatic retry at each phase.
3. Capture `window_end_ms` before searching. From the durable watermark, query Gmail through the routed integration with a one-second overlap using `in:inbox after:<epoch-seconds>`. Enumerate every page until `nextPageToken` is absent. Collect only Gmail `id`, `threadId`, and integer `internalDate` into the exact private `discovery_batch` path returned by `prepare-run`. If any page fails, do not call `discover-complete` and do not advance the watermark.
4. After complete enumeration, call `python3 <BASE_DIR>/scripts/inbox_monitor.py discover-complete --batch <batch-json> --window-start-ms <durable-watermark> --window-end-ms <captured-window-end-ms>`.
5. Repeatedly call `python3 <BASE_DIR>/scripts/inbox_monitor.py claim-next --claim-root <WORKSPACE>/estimate-desk/inbox-claims --stale-after-seconds 600`. This one command selects the oldest eligible item, claims or safely resumes it, synchronizes the queue, and returns the only supported persistent `work_paths`; never split those operations. Stop the loop when it returns `null`. Process only when `claim.acquired` is `true`; `claim.resumed:true` replays from durable state. Use the returned `work_paths` exactly. Never copy or choose a claim token, create `.jed-work`, `/tmp/jed-work`, or another ad-hoc work directory; never inspect or edit `claim.json`, `state.json`, queue files, or record JSON directly; and never use generic show/display/read-back calls for generated artifacts. Pass generated artifact paths directly between bundled scripts. Follow the installed SKILL.md rules exactly and record its required claim phases. Build ownership candidates only with `estimate_record.py lookup-thread`, and use the exact Gmail thread message count with `route_ownership.py`. A `new_inquiry` must have its retry-stable record persisted locally and mirrored to Kolo before any customer reply or terminal claim. Before deciding whether specifications are missing, fetch the exact full Gmail thread, read every message in chronological order, merge all customer-supplied facts, and persist `estimate_record.py record-thread-review` with every thread message ID plus the currently claimed source ID. Never evaluate only the newest email or re-ask a fact present anywhere in the thread. When required specifications remain missing at trust Stage 2 or 3, immediately send the price-free request in the original Gmail thread with `workflow_safe.py send-spec-followup`; do not ask whether to draft, send, or continue routing. When the full-thread specification is complete, calculate with the configured pricing model, create the complete owner-only cost sheet, and call `workflow_safe.py request-approval`. These high-level commands resolve the authoritative claim token, journal the external action, persist and mirror the record, advance phases, and finalize. Do not substitute `gmail_safe.py`, `kolo_safe.py`, direct `curl`, raw Gmail/Maton calls, `python -c`, or direct JSON edits. Never tell the owner to calculate or send manually. Never overwrite initial immutable evidence. Notification or `awaiting_specs` alone is not completion.
6. For every real customer reply on an active estimate, deliver the owner alert only through `python3 <BASE_DIR>/scripts/kolo_safe.py notify-owner-claimed`, which atomically performs notification write-ahead and records `sent` or `uncertain`. Never call `kolo notify-owner` directly for that event and never independently retry `pending` or `uncertain` notifications.
7. Never call `inbox_claim.py complete`, `inbox_claim.py fail`, `reconcile-terminal`, or `inbox_monitor.py finalize` separately after either successful high-level workflow command; it already finalizes. A send/build/persistence failure must become manual review, never processed. For every manual-review decision use the installed SKILL.md `kolo_safe.py manual-review-claimed` command. Conflicting terminal outcomes remain errors.
8. After `claim-next` returns null, run `python3 <BASE_DIR>/scripts/inbox_monitor.py assert-settled`. A nonzero result is a real run failure and must never be reported as `ok` or `NO_REPLY`. Stay silent with `NO_REPLY` only when this assertion passes and there is no error, manual-review item, or newly reconciled uncertain notification. Report only real errors, unresolved manual-review counts, or newly uncertain notification counts; never include customer data. For manual review, say exactly where the owner can act: `Ask Kolo to show unresolved Jewelry Estimate Desk reviews.` Never merely say `Open Kolo to review`.
