Run the Jewelry Estimate Desk inbox monitor exactly as follows.

Never call create_goal or update_goal. Goals do not apply in isolated cron sessions.

1. Validate <WORKSPACE>/estimate-desk/shop-profile.json. Run `python3 <BASE_DIR>/scripts/inbox_monitor.py status`. If activation_state is `prepared` or `reconfiguring`, exit successfully with `NO_REPLY` and do nothing else. Missing, corrupt, or unsupported state is a real error: fail closed without Gmail or customer side effects.
2. Before Gmail discovery, run `python3 <BASE_DIR>/scripts/inbox_claim.py notification-reconcile-stale --minimum-age-seconds 600`. This may only change stale `pending` owner notifications to `uncertain`; it must never deliver or retry a notification.
3. Capture `window_end_ms` before searching. From the durable watermark, query Gmail through the routed integration with a one-second overlap using `in:inbox after:<epoch-seconds>`. Enumerate every page until `nextPageToken` is absent. Collect only Gmail `id`, `threadId`, and integer `internalDate` into a private discovery batch. If any page fails, do not call `discover-complete` and do not advance the watermark.
4. After complete enumeration, call `python3 <BASE_DIR>/scripts/inbox_monitor.py discover-complete --batch <batch-json> --window-start-ms <durable-watermark> --window-end-ms <captured-window-end-ms>`.
5. Repeatedly call `python3 <BASE_DIR>/scripts/inbox_monitor.py next`. For each returned Gmail ID, call `python3 <BASE_DIR>/scripts/inbox_claim.py claim`, then immediately call `python3 <BASE_DIR>/scripts/inbox_monitor.py sync-claim` with the claim result before fetching message content or causing any side effect. Only process `acquired:true` claims. Follow the installed SKILL.md queue-processing, classification, route, ownership, pricing, approval, reply-thread, and confidentiality rules exactly.
6. For every real customer reply on an active estimate, deliver the owner alert only through `python3 <BASE_DIR>/scripts/kolo_safe.py notify-owner-claimed`, which atomically performs notification write-ahead and records `sent` or `uncertain`. Never call `kolo notify-owner` directly for that event and never independently retry `pending` or `uncertain` notifications.
7. Finish each claim first. Then call `python3 <BASE_DIR>/scripts/inbox_monitor.py reconcile-terminal --message-id <gmail-id> --claim-root <WORKSPACE>/estimate-desk/inbox-claims`.
8. Stay silent with `NO_REPLY` when there is no queued work, error, manual-review item, or newly reconciled uncertain notification. Report only real errors, manual-review items, or newly uncertain notification counts; never include customer data.
